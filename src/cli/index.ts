export { parseArguments } from './command-parser';
export {
  Command,
  GlobalOptions,
  CommitOptions,
  ConfigOptions,
  InitOptions,
  CommandOptions,
} from './types';

// Export other CLI-related functions as they're created
